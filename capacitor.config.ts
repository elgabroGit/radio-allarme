import { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'io.ionic.starter',
  appName: 'radio-allarme',
  webDir: 'dist',
  bundledWebRuntime: false
};

export default config;
